package ui

import androidx.compose.foundation.*
import androidx.compose.runtime.Composable

@OptIn(ExperimentalFoundationApi::class)
@Composable
fun CellDisplay(row: Int, col: Int) {
    /*val tooltip = remember { mutableStateOf(0) }
    val cell = common.Game.field.get(row, col)

    */
}